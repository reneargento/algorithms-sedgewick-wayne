3.4.14

The scenario A (all keys hash to the same index) and C (all keys hash to an even-numbered index) will lead to expected linear running time for a search miss in a linear-probing hash table. This assumes that in scenario C all keys hashed to the same even-numbered index.
