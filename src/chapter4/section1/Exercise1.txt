4.1.1

The maximum number of edges in a graph with V vertices and no parallel edges is V.
Example:
  o
 / \
o - o

The minimum number of edges in a graph with V vertices, none of which are isolated (have degree 0) is V - 1.
Example:
o - o - o
