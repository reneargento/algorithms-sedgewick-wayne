2.4.11

The most effective implementation would be an unordered array.
This is because the worst case bound for inserting in an unordered array is O(1), while it is O(log n) for a heap and O(n) for an ordered array.
这是因为在无序数组中插入的最坏情况是 O(1)，而对于堆是 O(log n)，对于有序数组是 O(n)。